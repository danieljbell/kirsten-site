<ul class="work--grid">
  {%- for post in collections.work -%}
    <li class="work--item {{post.data.color}}">
      <div class="work--image">
        <a href="{{post.url}}">
          <img src="{{post.data.icon}}" alt="{{ post.data.title }} thumbnail">
        </a>
      </div>
      <div class="work--content">
        <h4 class="work--title"><a href="{{post.url}}">{{ post.data.title }}</a></h4>
        <p class="work--excerpt">{{ post.data.excerpt | safe }}</p>
        <a href="{{post.url}}" class="button">Read More</a>
      </div>
    </li>
  {%- endfor -%}
</ul>